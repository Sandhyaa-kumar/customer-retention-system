# Frontend API URL for production.
# Use full URL if backend is on another domain, e.g. https://api.example.com
# Leave empty when frontend and backend are served from same origin/reverse-proxy.
VITE_API_URL=

# Optional: Dev proxy target (used by Vite dev server)
VITE_PROXY_TARGET=http://127.0.0.1:5000
